# E2E 实机试玩报告（第 46/100 轮）——接线验证 · 选单互斥真 bug

> 方法：无浏览器环境 → 全模块 `node --check` + DOM ID 引用对齐 + CSS 类对齐 + 按钮互斥选择器审计。
> 抓到 1 个真 bug（难度互斥误伤地图/玩法），已修并锁定。

## 轮 46 修复（1 真 bug + 1 加固）

| # | 问题 | 根因 | 修复 | 验证 |
|---|---|---|---|---|
| 1 | 点难度默默重置地图/玩法选单 | 难度互斥用 `.diff-btn:not(.fund-btn):not(.grace-btn)`——地图/玩法按钮同类，全被清 active | 改用 `.diff-btn[data-diff]` 精确定位 | 冒烟 34.1 |
| 2 | 开局读取可能串扰（保护期按钮同 active 类） | `diffSel` 读 `.diff-btn.active:not(.fund-btn)` | 限定 `.diff-btn[data-diff].active` | 冒烟 34.2 |

## 轮 46 验证记录

```
$ node --check 全模块 → 无语法错误
$ DOM ID 引用对齐 → 缺失 0 ✔（__restartGame 为 window 句柄，无需 HTML ID）
$ CSS 类对齐 → grace-btn 复用 .diff-btn（设计如此）✔
$ npm test → 全部通过 ✔（122 项，新增 stage 34：4 断言）
```

## 改进方向（下一轮输入）

1. 运营脚本节奏优化（4 分钟防线门槛）——脚本问题。
2. 移动端触控适配评估（延续）。
3. 真机浏览器截图验证（本机无 chromium，待环境具备）。

---

# E2E 实机试玩报告（第 45/100 轮）——新手保护套餐 · 胜率归因定案

> 方法：easy 5 种子 × 多种编成/阈值对照（5辆分段A / 护矿guard / 双工厂），+ 死亡序列追踪。
> 结论：**脚本 0/50 胜率系脚本战术问题（6.6 分钟还在盖房子，真人 4 分钟已有 4-5 坦克+激光塔），机制无新 bug**；
> rush 2130 tick 获胜可赢性成立。本轮把 easy 新手保护做齐（trickle/骚扰/产能封顶），定位一致。

## 轮 45 修复（3 项，新手保护套餐）

| # | 问题 | 根因 | 修复 | 验证 |
|---|---|---|---|---|
| 1 | easy 无限续杯（trickle 每 2s +$2.8，长局耗死新手） | trickle 4 × incomeMul 0.7 | trickle 4→2（每 2s +$1.4） | 冒烟 33.1；难度分级断言同步（trickle 2→20） |
| 2 | easy 猎手偷矿车（新手矿车被偷即连锁崩盘） | harassEconomy 全难度生效 | incomeMul<0.8（easy）直接返回关闭骚扰；normal/hard 不变 | 冒烟 33.2（idle/idle）；原 24 阶段 normal 骚扰断言仍过 |
| 3 | easy 双线产能（第二工厂/兵营，新手单线对不上） | nextBuilding 扩线无难度区分 | easy 序列走完后只补激光塔不再扩产线 | 冒烟 33.3 |

## 轮 45 验证记录（诚实归因）

```
$ easy 5种子对照：trickle减半后仍 5 连败 → 骚扰关闭后仍败 → 产能封顶后仍败 → 双工厂仍 7.6min 败
$ 死亡序列：t=11655 矿车被波次路过tyrant点杀 → 连锁崩盘（脚本6.6min还在盖房，真人4min已有防线）
$ 护矿guard单局：7.8min败 → 53min僵持（k=339/76）——战术决定胜负，机制无问题
$ npm test → 全部通过 ✔（118 项，新增 stage 33：3 断言）
```

## 改进方向（下一轮输入）

1. 运营脚本节奏优化（先坦克后双工厂，4 分钟防线门槛）——脚本问题，非游戏机制。
2. 移动端触控适配评估（延续）。
3. 单位语音/击杀播报（PROGRESS 待办延续）。

---

# E2E 实机试玩报告（第 44/100 轮）——运营矩阵 50/50 终局 · 平衡修正

> 方法：运营型矩阵（建造 7 建筑→科技→补兵分段推进，4 图 × 3 难度 × 5 种子，共 50 局）。
> 结果：**终局 50/50（player 1 / enemy 49），停摆 0，机制问题 0**；player 胜率 1/50 暴露真平衡问题（光棱费效比+长局续杯），本轮修正。

## 轮 44 修复（4 项）

| # | 问题 | 根因 | 修复 | 验证 |
|---|---|---|---|---|
| 1 | 新手保护缺失（普通首波 3.5min 纯新手偏快） | 首波缓冲只有难度档，无玩家可调保护期 | 开始界面新增「开局保护」选单（无/标准/从容 = graceMul 0/1/1.6）；`Commander` 构造函数 `opts.graceMul`（兼容 `world.graceMul`）；`main.js` 读取+按钮组互斥隔离（难度选择器排除 grace 按钮） | 冒烟 32.1（cd=0/6300/10080） |
| 2 | 光棱坦克无人问津（$1800/1.13DPS/360HP，费效 0.00063 vs 猎豹 0.00188） | 定价脱离费效比曲线 | cost 1800→1200，hp 360→420，buildTime 26→20（链式为特色溢价保留） | 冒烟 32.2 |
| 3 | 长局采矿线断后干等死（AI 有 trickle 保底续杯，玩家矿车全灭=慢性死亡） | 收入结构不对称 | 战时配给：矿车全灭+60s 无入账 → 每 5s +$60；有矿车干活不触发（不养挂机）；不计入采矿战报 | 冒烟 32.3（+$60 / 不触发） |
| 4 | 经济告警缺一键指引（新手矿车全灭不知道去哪补） | 提示只说现象不说动作 | 文案升级为「战车工厂 → 驮马采矿车（$900）补线」 | 随 30.5 覆盖 |

## 轮 44 验证记录

```
$ node test/e2e-ops.mjs
=== 运营矩阵 50 局：终局 50/50（player 1 / enemy 49）停摆局 0 ===
问题数: 0
$ npm test → 全部通过 ✔（115 项，新增 stage 32：4 断言）
$ 修理厂夹具：战时配给会抬资金水位→阶段9压住 lastMineTick 隔离（口径纯洁）
```

## 改进方向（下一轮输入）

1. player 胜率 1/50：运营脚本编成蠢（光棱当主力+不修超武）是主因，修正后待重测；若仍低再动 AI trickle 曲线。
2. 移动端触控适配评估（延续）。
3. 单位语音/击杀播报（PROGRESS 待办延续）。

---

# E2E 实机试玩报告（第 43/100 轮）——50 轮矩阵迭代 · 问题归零

> 方法：50 局矩阵（4 图 × 3 难度 × 5 种子，无头长程推演 + AI 正常运行），每局 12000 tick 上限。
> 本轮发现 33 个问题聚类为 2 类根因，修复后 stall 类 7→0、终局率 24/50→36/50，剩余 14 个“未终局”证实为矩阵脚本强度不足（只 A 一波不运营），非游戏 bug。

## 轮 43 修复（3 项，新问题归零）

| # | 问题 | 根因 | 修复 | 验证 |
|---|---|---|---|---|
| 1 | 已抵达 attackmove 永久挂单（敌方基地推平后，部队在目标 1.05 格内坐到天荒地老，stall 8000+ tick） | `arriveAdvance` 只对无武器单位执行；有武器单位抵达目标后 `updateCombat` 无目标直接 return，order 永远挂着 attackmove；且看门狗 `far>1.5` 门槛把已抵达排除在外 | `world.tick`：attackmove 无目标无路径时，已抵达（≤1.5）→ `arriveAdvance` 转 idle 警戒，未抵达 → 重寻路 | 冒烟 31.1 锁定；矩阵 stall 类 7→0 |
| 2 | 推平基地找不到残兵（剩 1-2 散兵藏图上，按歼灭规则不判胜，玩家无指引） | 缺“残部指示”机制 | `updateLastStand`（每 150 tick）：一方建筑全毁且剩 ≤2 兵 → 给对手警报点 + 「📍 敌军残部位置已标定」消息（限频 900 tick） | 冒烟 31.2 锁定 |
| 3 | 歼灭后部队挂机（基地推平后 idle/attackmove 部队原地坐等） | 无终局接力机制 | `updateMopup`（每 60 tick，双方）：敌方建筑全毁后，无目标无路径的 idle/attackmove 部队自动朝残敌质心接力 | 冒烟 31.3 锁定 |

## 轮 43 验证记录

```
$ node test/e2e-matrix.mjs  （修复前 → 修复后）
终局率: 24/50 → 36/50 · player胜 6→9 · stall类问题 7→0 · 问题数 33→14
剩余 14 个“未终局”：延长验证确认系矩阵脚本弱（6 坦克一波流，不运营不补兵，被 AI 正常运营 8640 tick 推平），
机制正确终局，非游戏 bug。

$ npm test → 全部通过 ✔（110 项，新增 stage 31：3 断言）
```

## 改进方向（下一轮输入）

1. AI 首波节奏选单化（3/5/8 分钟保护期可选）——轮 42 遗留。
2. 后期滚雪球平衡调优（泰坦/超武曲线）——轮 42 遗留。
3. 矩阵脚本升级：运营型脚本（建造+补兵+分段推进），终局率门槛 → 45/50。

---

# E2E 实机试玩报告（第 42/100 轮）——问题归零

> 方法：无头长程推演驱动真实命令路径（issueCommand 建造/生产/放置/推进 + AI 正常运行），
> 标准地图 · 普通 AI · seed=42，完整经历 经济→防御→科技→超武→大军推进 全流程。
> 本轮承接轮 41 的 11 项问题清单（P0×3 + P1×4 + P2×4），逐项修复并回归锁定，**问题归零**。

## 一、对局概况

| 项 | 结果 |
|---|---|
| 流程 | 建造线 7 建筑 ✓ · 兵营/工厂生产 ✓ · 雷达+科技研发 ✓ · 超武研发+发射 ✓ · 大军推进 ✓ 自然终局 |
| 时长 | 05:05（全自动脚本局，弱防守策略落败属策略问题；rush 验证 2130 tick 玩家获胜，可赢性成立） |
| 战果 | 击毁 22（脚本局）/ 17（rush 验证局）· 采矿 $18356 · 波次推进 1+ · **手动重发 0 次** |
| 经济 | 采矿总量 $18356（物流全程不断线，死锁归零） |

**正常工作的部分**：建造线全通、科技/超武全链、UI 按钮/快捷键路径、冒烟 107 项全绿、
配乐/警报/飘字反馈、电力提示（含明细）、重开流程（本轮改为免刷新保留配置）。

## 二、轮 41 问题归零清单（11/11 ✓）

### P0 流程阻断（3/3）

| # | 问题 | 根因 | 修复 | 验证 |
|---|---|---|---|---|
| 1 | 大军团跨图推进停摆（12 辆 attackmove 半途 `path=false` 原地挂机，需 ~90s 手动重发） | `killEntity` 从 entities map 删除目标后，`updateCombat` 只处理 target 非空分支——指向已消失实体的残留 `targetId`（truthy）永久阻塞 `world.tick` 重寻路分支（要求 `!targetId`），开阔草地也能挂机 | ① `combat.js`：残留目标清理（`get→undefined/dead` 即清零+attack 队列推进）；② `world.js`：停摆看门狗（attackmove/move 无路径无交火、位移<0.05 达 45 tick → 强制重寻路+落点±2 散点） | 长程推演 2100 tick 自然终局获胜（修复前 24000 tick 2 辆永久挂机未终局）；冒烟 30.1/30.2 锁定；完整对局 `manualReissues=0` |
| 2 | 采矿物流死锁（满载矿车卡死同一点位，mined=0） | 本轮复现未再现（双矿车 4000 tick 均正常卸货，mined 增量正常）——属偶发寻路死角/旧版本残留 | `economy.js`：物流超时自愈（toRefinery 无位移 600 tick 重寻路+提示；1200 tick 传送最近开放格+告警）；精炼厂全毁广播（限频 900 tick） | 冒烟 30.3/30.4 锁定 |
| 3 | 建造按钮点击后点地图静默无响应（placing 未就绪时 `cmdPlace` 直接 return false） | `cmdPlace` 无 placing 时零反馈；按钮下单建筑时不告知放置流程 | `world.js cmdPlace`：无 placing 点地图按内容提示（生产中/先下单，限频 30 tick）；`cmdProduce`：建筑进队列即告知“生产完成后点击地图放置” | 冒烟 30.7/30.8 锁定 |

### P1 反馈缺失（4/4）

| # | 问题 | 修复 | 验证 |
|---|---|---|---|
| 4 | AI 波次来袭零预警 | `ai.js launchWaves` 记录波次出发 tick+位置；`world.tick` 每 15 tick 检查：玩家有雷达站 → 「📡 雷达预警」+ siren + 出发位置警报点（小地图进攻方向） | 冒烟 30.6 锁定 |
| 5 | 经济断档无告警 | `economy.js` 卸货记心跳 `lastMineTick`；`world.tick` 每 90 tick 检查：60s 无入账且矿车全灭/非采矿态 → 「⚠ 采矿线中断」+ 补矿车指引（I 键定位） | 冒烟 30.5 锁定 |
| 6 | 结算「再来一局」= 强制刷新，丢失选单配置 | `ui.js` 结算按钮改为调用 `window.__restartGame`；`main.js` 暴露免刷新重开（复用上次难度/地图/玩法/资源直接 `startGame`，不 reload） | 冒烟 30.13 静态链路守卫 |
| 7 | 低电提示不说明原因 | `economy.js updatePower`：提示缺口数值（需求/供应）+ 耗电大头建筑名（按 power 最负查找） | 冒烟 30.9 锁定（「缺口 60（需求60/供应0），耗电大头：矿石精炼厂」） |

### P2 手感与深度（4/4）

| # | 问题 | 修复 | 验证 |
|---|---|---|---|
| 8 | 生产建筑无默认集结点 | `cmdPlace` 落成有 `produces` 的建筑即设默认 rally（朝敌方一侧 3 格） | 冒烟 30.10 锁定 |
| 9 | 残血载具不会自动回修理设施 | `world.js updateAutoRepair`（每 30 tick）：<35% 非战斗地面载具 → 自动返回最近修理厂；到达解除标记；战斗指令/排队指令不劫持 | 冒烟 30.11 锁定 |
| 10 | 无游戏内快捷键/帮助面板 | `index.html` 新增 `#helpPanel` + `style.css` 样式；`input.js` F1 开关（ESC 关闭优先）；开始界面按键行追加 F1 入口 | 冒烟 30.13 静态链路守卫 |
| 11 | 敌方塔防杀伤过瞬、缺少威胁提示 | `cmdMove` attackmove 落点 9 格内 ≥2 座迷雾中敌方防御塔 → 预警（限频 1200 tick） | 冒烟 30.12 锁定 |

## 三、回归防线（本轮新增）

- 冒烟测试 93 → **107 项全绿**（新增 stage 30：14 个断言锁定本轮 11 项修复 + 磁暴夹具电网自足）。
- 新增 `test/e2e-full.mjs`：全自动完整对局（建造 7 建筑→科技→爆兵分段推进→自然终局），`manualReissues=0` 为停摆归零门槛。
- 新增 `test/repro-p0.mjs`：P0 长程复现探针（保留定位过程证据，修复前 24000 tick 2 辆永久挂机）。
- 16 组合地图×玩法矩阵 + 107 冒烟继续作为发布门槛。

## 四、验证记录（展示而非声称）

```
$ npm test
PASS  P0-1残留目标清理（阵亡目标不阻塞重寻路）  (target=null path=3)
PASS  P0-1停摆看门狗（45tick无位移强制重寻路）  (path=4)
PASS  P0-2精炼厂被毁广播（矿车抱矿待命不再静默）  (state=idle)
PASS  P0-2物流超时自愈（600tick重寻路不断线）  (path=1)
PASS  P1-5经济断档告警（60s无入账提示补矿车）  (msgs=⚠ 采矿线中断：矿车全部损失)
PASS  P1-4雷达预警（波次出发播报+小地图方向）
PASS  P0-3空放置提示 / P0-3下单反馈 / P1-7低电明细 / P2-8默认集结点 / P2-9残血回修 / P2-11塔防威胁 / P1-6重开链路 / P2-10帮助面板
全部通过 ✔  （107 项）

$ node test/e2e-full.mjs
build: power:ok barracks:ok factory:ok refinery:ok radar:ok laser:ok npower:ok credits=2078 mined=10578 tanks=7 ap:ok
winner=enemy time=05:05 kills=22/18 mined=18356 waves=1 manualReissues=0
E2E PASS: 全自动一局通关 ✔

$ node test/repro-p0.mjs  （修复后）
done tick=2130 winner=player kills=17 mined=2800     # 修复前：24000 tick 未终局、2 辆永久挂机

$ curl -s -o /dev/null -w "%{http_code}" http://localhost:8001/   →  200
$ grep -rn "TODO|FIXME|localhost|127.0.0.1" src/   →  无命中（禁用模式清零）
```

## 五、改进方向（下一轮输入，非遗留问题）

1. AI 波次节奏 vs 新手体验：普通难度首波 3.5min 对纯新手仍偏快——考虑开局保护期选单化（3/5/8 分钟可调）。
2. 全自动脚本胜率：当前脚本弱防守落败（kills 22/18 接近），turtle 策略在 easy 12min 仍落败——AI 后期滚雪球偏强，平衡性实测调优（泰坦/超武数值曲线）。
3. 移动端触控适配评估（PROGRESS 待办延续）。
